#include "main.h"
/**
* print_sign - check if sign of n
* @n: parameter
*
* Return: 0 Always (Success)
*/

int print_sign(int n)
{
if (n = 0)
{
_putchar(0/10 + '0');
}
else if (n > 0)
{
_putchar('+');
}
else
{
putchar('-');
}
return (n > 0 || n < 0);
}
