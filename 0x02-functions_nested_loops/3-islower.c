#include "main.h"
#include <stdlib.h>
/**
* _islower - check if c is lowercase
* @c:parameter
*
* Return: 0 Always (Success)
*/

int _islower(int c)
{
if (c.islower)
{
return (1);
}
else
{
return (0);
}
}
