#include "main.h"

/**
* main - display information containes
* in the ELF header at the start of an ELF file.
* @argc: argument count
* @argv: arguments pointers
*
* Return: 0 always
*/

int main(char argc, int *argv[])
{
	return (0);
}
