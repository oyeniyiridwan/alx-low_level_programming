#include "main.h"
#include <stdio.h>

/**
* main - tests function that prints if integer is positive or negative
* Return: 0
*/

int main(void)
{
char str[16] = "My first strlen!";
rev_string(str);
printf("%s\n",str);
return (0);
}
