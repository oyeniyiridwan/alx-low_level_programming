#include "main.h"
#include <stdio.h>

/**
* main - tests function that prints if integer is positive or negative
* Return: 0
*/

int main(void)
{
int a;
a = largest_number(2, 2, 2);
printf("%d largest", a);
return (0);
}
