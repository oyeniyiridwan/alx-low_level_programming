#include <stdio.h>

/**
* main - print its name
* @argc: parameter
* @argv: parameter
*
* Return: Always o (Success)
*/


int main(int argc, char **argv)
{
if (argc >= 1)
{
printf("%s\n", argv[0]);
}
return (0);
}
